# ===================================================================
#  Creative Catalyst Engine - Production Dependencies
# ===================================================================

# --- API & Web Server ---
fastapi
uvicorn[standard]
sse-starlette # For real-time server-sent events
sseclient-py      # For handling Server-Sent Events (SSE) streams.



# --- Background Task Queue & Caching ---
arq
# redis[hiredis] is used for performance and async support is built-in.
redis[hiredis]
async-timeout   # Explicit dependency for older redis versions, safe to keep for now.

# --- Core AI & API Communication ---
google-genai
google-api-core
pydantic

# --- Vector Database Caching ---
chromadb

# Dall-E image generation
openai

# --- Observability ---
sentry-sdk[fastapi,arq]

# --- Utilities ---
python-dotenv
PyYAML
python-json-logger
pillow
anyio
requests
        # For the standalone api_client

pytest
pytest-asyncio
pytest-mock
pytest-cov
pytest-snapshot